---
applications:
  - name: pe-manager
    memory: 256M
    disk_quota: 512M
    instances: 1
    buildpacks:
      - staticfile_buildpack
    path: dist
    stack: cflinuxfs4
    env:
      FORCE_HTTPS: true
    # Uncomment and update with your actual route
    # routes:
    #   - route: pe-manager.cfapps.sap.hana.ondemand.com